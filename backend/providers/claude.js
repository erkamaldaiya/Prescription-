// Phase 2: Anthropic Claude provider
// Install: npm install @anthropic-ai/sdk
// Then replace this stub with the real implementation

export async function getClaudeSuggestion(_field, _text, _context) {
  throw new Error('Claude provider is not yet configured. Add your ANTHROPIC_API_KEY to .env and implement this provider.');
}
